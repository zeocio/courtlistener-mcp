#!/usr/bin/env python3
"""
Unit Tests for CourtListener MCP PositionTools

Comprehensive testing of get_positions functionality including:
- Success scenarios with various parameters
- Error handling (401, 403, 404, 429, 500, timeout, network)
- Parameter validation and edge cases
- Performance requirements
- Data formatting validation

Generated by: tests/scripts/generate_unit_tests.py
"""

import pytest
import httpx
import time
from unittest.mock import AsyncMock, patch, MagicMock
from mcp.server.fastmcp import FastMCP

from tools.position_tools import register_position_tools


class TestPositionTools:
    """Unit tests for get_positions - Retrieve judicial positions and appointments"""
    
    @pytest.fixture
    async def mcp_server(self):
        """Create MCP server with position_tools registered."""
        mcp = FastMCP(name="test")
        register_position_tools(mcp)
        return mcp
    
    @pytest.fixture
    def mock_api_response(self):
        """Standard mock API response."""
        return {
            "count": 1,
            "results": [{
                "id": 12345,
                "date_created": "2020-01-15T10:30:00Z",
                "date_modified": "2020-01-15T10:30:00Z"
            }]
        }

    @pytest.mark.unit
    async def test_get_positions_success(self, mcp_server, mock_api_response):
        """Test successful get_positions execution."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.json.return_value = mock_api_response
            mock_get.return_value.status_code = 200
            
            result = await mcp_server.get_tool('get_positions').call(
                position_id=12345, person_id=8521, court_id="scotus"
            )
            
            # Verify successful execution
            assert isinstance(result, str)
            assert len(result) > 0
            assert mock_get.called

    @pytest.mark.unit
    async def test_get_positions_not_found(self, mcp_server):
        """Test get_positions with no results found."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.json.return_value = {"count": 0, "results": []}
            mock_get.return_value.status_code = 200
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert "No" in result or "not found" in result.lower()
    
    @pytest.mark.unit
    async def test_get_positions_auth_error(self, mcp_server):
        """Test get_positions authentication error handling."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.status_code = 401
            mock_get.return_value.json.return_value = {
                "detail": "Authentication credentials were not provided."
            }
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert "authentication" in result.lower() or "unauthorized" in result.lower()
    
    @pytest.mark.unit
    async def test_get_positions_forbidden_error(self, mcp_server):
        """Test get_positions forbidden error handling."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.status_code = 403
            mock_get.return_value.json.return_value = {
                "detail": "You do not have permission to perform this action."
            }
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert "permission" in result.lower() or "forbidden" in result.lower()
    
    @pytest.mark.unit
    async def test_get_positions_rate_limit_error(self, mcp_server):
        """Test get_positions rate limiting error handling."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.status_code = 429
            mock_get.return_value.json.return_value = {
                "detail": "Request was throttled. Expected available in 60 seconds."
            }
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert "rate" in result.lower() or "throttled" in result.lower()
    
    @pytest.mark.unit
    async def test_get_positions_server_error(self, mcp_server):
        """Test get_positions server error handling."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.status_code = 500
            mock_get.return_value.text = "Internal Server Error"
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert "error" in result.lower() or "server" in result.lower()
    
    @pytest.mark.unit
    async def test_get_positions_network_timeout(self, mcp_server):
        """Test get_positions network timeout handling."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.side_effect = httpx.TimeoutException("Request timeout")
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert "timeout" in result.lower() or "network" in result.lower()
    
    @pytest.mark.unit
    async def test_get_positions_connection_error(self, mcp_server):
        """Test get_positions connection error handling."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.side_effect = httpx.ConnectError("Connection failed")
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert "connection" in result.lower() or "network" in result.lower()

    @pytest.mark.unit
    async def test_get_positions_parameter_validation(self, mcp_server):
        """Test get_positions parameter validation."""
        # Test with valid parameters
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.json.return_value = {"count": 0, "results": []}
            mock_get.return_value.status_code = 200
            
            result = await mcp_server.get_tool('get_positions').call()
            
            # Should not raise exception with valid params
            assert isinstance(result, str)

    
    @pytest.mark.unit
    @pytest.mark.parametrize("limit_value", [-1, 0, 1001])
    async def test_get_positions_invalid_limit(self, mcp_server, limit_value):
        """Test get_positions with invalid limit values."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.json.return_value = {"count": 0, "results": []}
            mock_get.return_value.status_code = 200
            
            # Some invalid limits might be handled gracefully
            result = await mcp_server.get_tool('get_positions').call(limit=limit_value)
            assert isinstance(result, str)

    
    @pytest.mark.unit
    async def test_get_positions_invalid_id_types(self, mcp_server):
        """Test get_positions with invalid ID types."""
        with patch('httpx.AsyncClient.get') as mock_get:
            # Test should handle invalid IDs gracefully
            mock_get.return_value.json.return_value = {"count": 0, "results": []}
            mock_get.return_value.status_code = 200
            
            # Test with string instead of int for ID fields
            result = await mcp_server.get_tool('get_positions').call()
            assert isinstance(result, str)

    @pytest.mark.unit
    @pytest.mark.slow
    async def test_get_positions_performance(self, mcp_server, mock_api_response):
        """Test get_positions performance requirements."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.json.return_value = mock_api_response
            mock_get.return_value.status_code = 200
            
            start_time = time.time()
            result = await mcp_server.get_tool('get_positions').call()
            end_time = time.time()
            
            # Should complete within reasonable time (5 seconds max)
            duration = end_time - start_time
            assert duration < 5.0, f"Tool took too long: {duration:.2f}s"
            assert isinstance(result, str)
            assert len(result) > 0
    
    @pytest.mark.unit
    async def test_get_positions_large_result_handling(self, mcp_server):
        """Test get_positions with large result sets."""
        # Mock large response
        large_response = {
            "count": 1000,
            "results": [{"id": i, "data": "x" * 100} for i in range(100)]
        }
        
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.json.return_value = large_response
            mock_get.return_value.status_code = 200
            
            result = await mcp_server.get_tool('get_positions').call(limit=100)
            
            # Should handle large responses without issues
            assert isinstance(result, str)
            assert len(result) > 1000  # Should have substantial content

    @pytest.mark.unit
    async def test_get_positions_empty_response(self, mcp_server):
        """Test get_positions with empty API response."""
        with patch('httpx.AsyncClient.get') as mock_get:
            mock_get.return_value.json.return_value = {"count": 0, "results": []}
            mock_get.return_value.status_code = 200
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert isinstance(result, str)
            # Should gracefully handle empty results
            assert len(result) > 0  # Should provide meaningful message
    
    @pytest.mark.unit
    async def test_get_positions_malformed_response(self, mcp_server):
        """Test get_positions with malformed API response."""
        with patch('httpx.AsyncClient.get') as mock_get:
            # Malformed JSON response
            mock_get.return_value.json.side_effect = ValueError("Invalid JSON")
            mock_get.return_value.status_code = 200
            mock_get.return_value.text = "Invalid JSON response"
            
            result = await mcp_server.get_tool('get_positions').call()
            
            assert isinstance(result, str)
            assert "error" in result.lower() or "invalid" in result.lower()
